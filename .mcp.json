{
  "mcpServers": {
    "agent-media": {
      "command": "npx",
      "args": [
        "-y",
        "@agentmedia/mcp-server@latest"
      ],
      "env": {
        "AGENT_MEDIA_API_KEY": "${AGENT_MEDIA_API_KEY}"
      }
    }
  }
}
